package com.fzt.learning.android.linearlayout;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        /* Convertire il Layout di default con
        un LinearLayout (verticale).
        Poi aggiungere una casella di testo (TextView)
        e una immagine (ImageView) come fatto
        nelle precedenti applicazioni.

        Creare le risorse necessarie per il testo
        (testo, colore, dimensione)
        e quelle per l'immagine (immagine stessa)
        */
    }
}
